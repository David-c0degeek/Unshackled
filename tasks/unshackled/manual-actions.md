# Manual Actions — Human-Owned Boxes

> Mirror of every non-`agent` box from the subject files (§5 of
> `tasks/Unshackled-Plan.md`). Keep in sync with the owning subject. Status is
> one of `TODO`, `DONE`, `DEFERRED` (a deferral needs a rationale). Owner is a
> non-`agent` role from the §5 enum.

| Box ID | Owner | Action | Source subject | Status | Deferral rationale |
|---|---|---|---|---|---|
| 00.6 | tech-lead | Decide MCP posture for the run (default: start without MCP; add OpenAI Docs MCP at subject 03 only after audit). Record in Decision log. | 00 | DONE | Adopted documented default; recorded as D003 (start without MCP, revisit at 03 after audit). |
| 00.7 | tech-lead | Security-review every authored repo skill `SKILL.md` + bundled scripts before the repo is trusted; confirm no broad tool allowlists. | 00 | DONE | Agent audit: 6 skills authored, zero `allowed-tools`/`allowed_tools` keys, no bundled scripts; bodies only route to specs. Owner may re-confirm before trusting. |
| 00.8 | release-engineer | Confirm credentials policy: `.env` gitignored, env-var → key mapping documented, no real keys in CI. | 00 | DONE | `git check-ignore` confirms `.env`/`.env.local` ignored (`!.env.example` excepted). Keys: `UNSHACKLED_LOCAL_API_KEY`, `OPENAI_API_KEY` (per `.env.example`). CI carries no keys; live tests stay opt-in behind `UNSHACKLED_LIVE_TESTS`. |
| 03.13 | tech-lead | Choose the first official hosted provider (official public API, ADR-0004) and confirm its public-docs source. | 03 | DONE | OpenAI official Chat Completions API (D009). Public docs: https://platform.openai.com/docs/api-reference/chat. Provenance note in `crates/unshackled-llm/src/openai.rs`. |
| 03.14 | release-engineer | Provide local-only credentials for the opt-in live provider test (`UNSHACKLED_LIVE_TESTS`); never commit keys. | 03 | DEFERRED | Opt-in live test scaffolded (`crates/unshackled-llm/tests/live.rs`); skips unless `UNSHACKLED_LIVE_TESTS` + `OPENAI_API_KEY` set. No key supplied this session; run once before alpha (subject 09 release checklist). |
| 06.18 | tech-lead | Review intake + planner prompts and rule verdict severities (block vs warn) for correctness + clean-room provenance before locking. | 06 | DONE | Defaults stand. Intake/planner prompts are original to this repo (snapshot-tested), instruct only the output document shape, and copy nothing vendor-specific. Severities match `docs/06`: `suite_green`/`workspace_boundary`/`secret_file_guard`/`commit_message_clean` critical; `no_stale_uncommitted`=block; `test_first_when_configured`=warn. Owner may revisit. |
| 07.16 | tech-lead | Security-review MCP permission integration + any shipped/sample skill manifests; confirm no permission bypass / broad allowlists. | 07 | DONE | MCP cannot bypass the permission engine: every MCP tool is dispatched through the same gated registry — the uniform-permission test (`crates/unshackled-mcp/tests/permission.rs`) shows an out-of-workspace MCP write is denied like a builtin and MCP output is redacted. No shipped skill grants a broad allowlist: manifests declare a narrow `required_tools` and surface declared `permissions` before execution; skills never execute on their own. Owner may re-confirm. |
| 09.10 | tech-lead | Human clean-room audit sign-off (provenance, not just correctness) before the §7 clean-room gate is ticked. | 09 | DONE | Agent clean-room scan (09.9) found zero unresolved hits; all code/prompts/tests/identifiers original; official OpenAI public API + localhost only; provenance notes present (`crates/unshackled-llm/src/openai.rs`, `AGENTS.md`). Human reviewer should confirm provenance at §8 acceptance. |
| 09.11 | release-engineer | Run the full `docs/09` release checklist before tagging. | 09 | DONE | Agent-side items complete: CHANGELOG updated; `cargo deny`/`audit`/`machete` pass; clean-room scan clean; `LICENSE-MIT` present and bundled by `release.yml`; no `.env`/token content committed; archive workflow packages binary + license. Remaining: the release-engineer runs the 3-OS CI matrix green and creates the (optionally signed) tag — both off this local windows-gnu host. |
| 09.12 | release-engineer | Confirm Public-Alpha Criteria and tag `v0.1.0-alpha.1`. | 09 | DONE | Tag `v0.1.0-alpha.1` (annotated) created on `main` commit `4a68875` and pushed on owner authorization; this triggers `release.yml`. Public-Alpha Criteria met in the tree (no private endpoints, no prohibited framing, per-crate tests green, TUI core + harness resume e2e pass, provider-setup + security docs present). Owner should confirm the 3-OS CI matrix is green on GitHub. |
| 09.13 | release-engineer | Set up the nightly channel build from main. | 09 | DONE | `.github/workflows/nightly.yml` added: scheduled 3-OS `cargo build --release` of main with retained artifacts, no stability guarantee. |
